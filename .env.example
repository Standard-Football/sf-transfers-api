# URL of the upstream proxy that already aggregates Sportmonks transfer
# data and is keyed by Opta playerId. The default is the production
# sf-consumer-fe deployment; override only if a staging upstream needs
# to be tested.
UPSTREAM_URL=https://sf-consumer-fe.vercel.app/api/consumer
