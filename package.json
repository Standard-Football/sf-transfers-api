{
  "name": "sf-transfers-api",
  "version": "0.1.0",
  "private": true,
  "description": "Public REST bridge for player transfer history. Wraps the sf-consumer-fe upstream and exposes a clean GET endpoint for external consumers.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/node": "^3.0.17",
    "typescript": "^5.3.3"
  }
}
